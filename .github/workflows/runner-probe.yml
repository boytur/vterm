name: runner-probe
# TEMPORARY: isolates the self-hosted runner crash. Delete after diagnosis.
on:
  push:
    branches: [feat/split-panes]
    paths: [.github/workflows/runner-probe.yml]
jobs:
  probe:
    runs-on: [self-hosted, Windows, X64, vterm-win]
    steps:
      - run: echo hello-from-vterm-win
